{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "seed": "node seeder.js",
    "wipe": "node -e \"import('dotenv/config');import('./models/Product.js').then(async m=>{const P=m.default;(await import('mongoose')).default.connect(process.env.MONGO_URI).then(()=>P.deleteMany({}).then(()=>{console.log('Wiped');process.exit(0)}))})\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.18.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
